<manifest xmlns:android="http://schemas.android.com/apk/res/android"

          package="com.adwo.sample"
>


    <!-- 手机网络访问权限 必须权限-->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- 手机状态访问权限  必须权限-->
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <!-- 大略位置权限  必须权限-->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <!-- SD卡写入权限  必须权限 -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />


    <!-- 设备震动权限  可选权限 -->
    <uses-permission android:name="android.permission.VIBRATE" />
    <!-- 识别网络状态权限 可选权限 -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- 摄像头权限 可选权限 -->
    <uses-permission android:name="android.permission.CAMERA" />
    <!-- 录音权限  可选权限-->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <!-- 识别wifi状态权限 可选权限 -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />


    <application android:allowBackup="true"
                 android:label="@string/app_name"
                 android:supportsRtl="true"
    >

        <!-- 如果你使用了Google服务，需要注册所使用版本号。 -->
        <meta-data android:name="com.google.android.gms.version" android:value="5077000" />
        <!--<activity-->
            <!--android:name="com.adwo.sample.EntryActivity"-->
            <!--android:label="@string/app_name" >-->
            <!--<intent-filter>-->
                <!--<action android:name="android.intent.action.MAIN" />-->

                <!--<category android:name="android.intent.category.LAUNCHER" />-->
            <!--</intent-filter>-->
        <!--</activity>-->
        <activity
            android:label="@string/app_name"
            android:hardwareAccelerated="true"
            android:name="com.adwo.sample.NativeActivity"
            />
        <activity
            android:label="@string/app_name"
            android:hardwareAccelerated="true"
            android:name="com.adwo.sample.FeedsActivity"
            />


        <activity
            android:label="@string/app_name"
            android:hardwareAccelerated="true"
            android:name="com.adwo.sample.InterstitialActivity"
            />
        <activity
            android:label="@string/app_name"
            android:launchMode="singleTask"

            android:configChanges="keyboard|keyboardHidden|screenLayout|uiMode|screenSize"
            android:name="com.adwo.sample.BannerActivity" >
        </activity>
    </application>

</manifest>
